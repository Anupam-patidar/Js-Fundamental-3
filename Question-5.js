// QUestion:Write a JavScript program that calculates the area of a circle using the Math object's properties and
// methods.

// Ans:-



let radius=10;

let areaOfCircle = Math.PI*Math.pow(radius,2);
console.log(areaOfCircle);
